services:
  postgres:
    image: postgres:17-alpine
    container_name: fms-postgres
    restart: always
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    # 프로덕션: DB 포트 외부 노출 안 함
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - internal

  backend:
    build:
      context: ./backend
    container_name: fms-backend
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      FLASK_APP: run.py
      FLASK_ENV: production
      FLASK_DEBUG: "0"

      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"

      # AWS S3: IAM Role 사용 시 빈 값으로 명시 (config.py 기본값 덮어쓰기 위함)
      S3_ENDPOINT_URL: ""
      S3_ACCESS_KEY: ""
      S3_SECRET_KEY: ""
      S3_BUCKET_NAME: ${S3_BUCKET_NAME}
      S3_REGION: ${S3_REGION:-ap-northeast-2}
      S3_USE_SSL: "true"

      # Cognito (Phase 2) — 컨테이너로 명시 전달 필수
      COGNITO_REGION: ${COGNITO_REGION:-ap-northeast-2}
      COGNITO_USER_POOL_ID: ${COGNITO_USER_POOL_ID}
      COGNITO_CLIENT_ID: ${COGNITO_CLIENT_ID}
      AUTH_DEV_BYPASS: "false"
    # 포트 외부 노출 없음 (Nginx가 내부 프록시)
    networks:
      - internal
    # Gunicorn: workers = (2 * CPU) + 1 권장, 최소 2
    command: >
      gunicorn
        --bind 0.0.0.0:5000
        --workers ${GUNICORN_WORKERS:-3}
        --timeout 120
        --access-logfile -
        --error-logfile -
        run:app

  frontend:
    build:
      context: ./frontend
      target: prod
      args:
        VITE_API_BASE_URL: /api
        VITE_COGNITO_REGION: ${COGNITO_REGION:-ap-northeast-2}
        VITE_COGNITO_USER_POOL_ID: ${COGNITO_USER_POOL_ID}
        VITE_COGNITO_CLIENT_ID: ${COGNITO_CLIENT_ID}
        VITE_COGNITO_DOMAIN: ${COGNITO_DOMAIN}
        VITE_COGNITO_REDIRECT_URI: https://${DOMAIN}/auth/callback
    container_name: fms-frontend
    restart: always
    networks:
      - internal

  nginx:
    image: nginx:alpine
    container_name: fms-nginx
    restart: always
    depends_on:
      - backend
      - frontend
    ports:
      - "80:80"
      - "443:443"
    environment:
      - DOMAIN=${DOMAIN}
    volumes:
      - ./infra/nginx/prod.conf:/etc/nginx/templates/default.conf.template
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - certbot_webroot:/var/www/certbot
    networks:
      - internal

volumes:
  postgres_data:
  certbot_webroot:

networks:
  internal:
    driver: bridge
